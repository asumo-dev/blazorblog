This is an example project of using [Ghost](https://ghost.org/) as a data source for BlazorBlog.

## Settings

Configure the following settings in `wwwroot/appsettings.json`.

```json
{
  "Ghost": {
    "ApiUrl": "{API_URL}",
    "ContentApiKey": "{CONTENT_API_KEY}"
  }
}
```
